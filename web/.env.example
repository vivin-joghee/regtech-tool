# Optional: explicit API base when not using Vite's dev proxy.
# Leave blank in dev (Vite proxies /api -> http://localhost:8787 — see vite.config.ts).
# Set in Cloudflare Pages env vars when deploying:
#   VITE_API_BASE=https://strait-compliance-worker.<account>.workers.dev
VITE_API_BASE=
